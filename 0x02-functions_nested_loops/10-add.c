#include "main.h"

/**
 * adds two integers and returns the result.
 * Ga: int to be added to b
 * Gb: int to be added to a
 * Return: sum of a and b
 */
int add(int, int)
{
	int c;
	c = a + b;
	return (c);
}
