#include "main.h"

/**
 *  function that computes the absolute value of an integer.
 *  Gc: the int to check
 *  Return: the  absolute value of int
 */
int _abs(int n)
{
	if (n >=0)
	{
	return (n);
	}
	return (-n);
}

